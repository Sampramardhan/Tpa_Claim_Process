# Database

PostgreSQL initialization scripts live in `init/`.

The scripts currently create schema boundaries only. Tables and relational models should be added later when the domain workflows are designed.
