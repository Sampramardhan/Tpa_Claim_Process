# Backend

Spring Boot foundation for the TPA claim processing API.

## Run

```bash
mvn spring-boot:run
```

## Package Layout

The domain packages are placeholders only. Controllers, services, repositories, DTOs, and entities should be added later as real workflows are defined.
